---
interface SectionContainerProps {
    id?: string;
    className?: string;
}
const {className, id}: SectionContainerProps = Astro.props
---

<section id={id} class={`w-full mx-auto lg:w-[740px] ${className ?? ''}`}>
    <slot />
</section>