---
interface SectionContainerProps {
    id?: string;
    className?: string;
}
const {id, className }:SectionContainerProps = Astro.props
---

<section id={id} class={`w-full mx-auto lg:w-[740px] ${className ?? ''}`}>
    <slot />
</section>